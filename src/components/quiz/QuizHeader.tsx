import { Cross, BookOpen } from "lucide-react";
export function QuizHeader() {
  return <header className="text-center py-8 px-4 animate-fade-in">
      <h1 className="sr-only">Quiz: Como é a sua vivência católica? | Bom Católico</h1>
    </header>;
}